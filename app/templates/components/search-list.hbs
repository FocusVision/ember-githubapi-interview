{{#if query}}
  {{#each model as |user|}}
    <div class='list-item'>
      <img
        class='list-avatar'
        src={{user.avatarUrl}}
      />
      {{user.login}}
    </div>
  {{else}}
    <div class='list-empty'>
      Nope
    </div>
  {{/each}}
{{/if}}
